name: CI golang

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '2 * * * *'
jobs:
  call-workflow-merge:
    needs: call-workflow-check
    uses: ivvorg/public-test-repo/.github/workflows/mergeprs.yml@main
