name: Check PR and Merge PR

on: [pull_request_target]

jobs:
  call-workflow-check:
    uses: ivvorg/public-test-demo/.github/workflows/check.yml@main
  call-workflow-merge:
    needs: call-workflow-check
    uses: ivvorg/public-test-demo/.github/workflows/mymerge.yml@main
    secrets:
      personaltoken: ${{ secrets.PERSONAL_TOKEN }}
