name: Unlink issue from Milestone

on:
  issues:
    types: [reopened]

jobs: 
  link:
    runs-on: ubuntu-latest

    steps:
    - name: Unlink tissue from milestone
      env: 
        GH_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
        repo: ${{ GITHUB.REPOSITORY }}
        issue: ${{ github.event.issue.number }}
      run: curl -s https://raw.githubusercontent.com/ivvist/ci-action/master/scripts/unlinkmilestone.sh | bash 

