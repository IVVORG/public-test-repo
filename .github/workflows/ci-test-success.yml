name: Test success

on: [push]

jobs:
  call-workflow-show_time:
    uses: ivvorg/public-test-repo/.github/workflows/show_time.yml@main

  display-time:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Show current
      run: echo "The cur time $(date)"

   	
